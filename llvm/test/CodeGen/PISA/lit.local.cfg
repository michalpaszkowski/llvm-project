if "PISA" not in config.root.targets:
    config.unsupported = True

# PISA tests can opt out of optional optimization passes via this substitution;
# it expands to nothing in upstream builds.
config.substitutions.append(("%disable_optional_optimizations", ""))
